## Packages
framer-motion | Smooth page transitions and element animations
date-fns | Formatting dates for news articles

## Notes
Authentication uses session cookies (credentials: include).
News content might contain HTML, use safe rendering.
Images will be external URLs from news sources or Unsplash placeholders.
